#include <stdio.h>

int main() {
    float base, altura, area Tri;

    printf("Introduce la medida de la base del triángulo: ");
    scanf("%f", &base);

    printf("Introduce la altura del triángulo: ");
    scanf("%f", &altura);

    area Tri = (base * altura) / 2.0;

    printf("El área calculada es: %.2f\n", area Tri);

    return 0;
}
